<UserControl x:Class="Phrike.GroundControl.Views.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Phrike.GroundControl.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="700">
    <UserControl.Resources>
        <viewModels:SettingsViewModel x:Key="SettingsViewModel" />
    </UserControl.Resources>

    <Grid Margin="0" DataContext="{StaticResource SettingsViewModel}">
        <Label Content="Sensor COM Port" HorizontalAlignment="Left" Margin="126,87,0,0" VerticalAlignment="Top"/>
        <ComboBox HorizontalAlignment="Left" Margin="282,87,0,0" VerticalAlignment="Top" Width="120" Name="SensorComPortComboBox" SelectedValue="{Binding Mode=OneWayToSource, Path=SensorComPort}" />
    </Grid>
</UserControl>
